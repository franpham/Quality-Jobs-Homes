
extends ./layouts/mainLayout
include ./mixins/templater

block content
  h1(style="text-align: center;") Data for Miami, FL

  h2 Rental Stats

  ul(id="rental-stats")
    +listGroupDetails(rentals)

  h2 Home Stats

  ul(id="home-stats")
    +listGroupDetails(homes)

  h2 Job Listings

  ul(id="job-listings")
    +listUniqueData(jobs)
